baseURL: "https://investnest.blog/"
languageCode: "en-us"
title: "InvestNest"
theme: "PaperMod"

outputs:
  home:
    - HTML
    - RSS
    - JSON

menu:
  main:
    - name: Search
      url: search/
      weight: 10

params:
  defaultTheme: "auto"
  ShowReadingTime: true
  ShowPostNavLinks: true
  showPoweredBy: false
  hideFooter: false

  # Explicitly force the favicon paths
  assets:
    favicon: "/favicon.ico"
