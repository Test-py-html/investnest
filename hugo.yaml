baseURL: "https://investnest.blog/"
languageCode: "en-us"
title: "InvestNest"
theme: "PaperMod"

outputs:
  home:
    - HTML
    - RSS
    - JSON

menu:
  main:
    - name: Search
      url: search/
      weight: 10

params:
  defaultTheme: "auto"
  ShowReadingTime: true
  ShowPostNavLinks: true
  showPoweredBy: false
  hideFooter: false

  # RealFaviconGenerator favicon configuration
  assets:
    favicon: "/favicon.ico"
  
  # Additional favicon link references handled in template
