baseURL: "https://investnest.blog/"
languageCode: "en-us"
title: "InvestNest"
theme: "PaperMod"

outputs:
  home:
    - HTML
    - RSS
    - JSON # Required for the search index engine

menu:
  main:
    - name: Search
      url: search/
      weight: 10

params:
  defaultTheme: "auto"
  ShowReadingTime: true
  ShowPostNavLinks: true
  showPoweredBy: false # Completely removes the Hugo & PaperMod watermark links
  hideFooter: false
